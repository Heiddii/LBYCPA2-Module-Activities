#include <stdio.h>

int phase2(void)
{ int itemCode, quantity;
char nextPhase = "";
int cost = 0;
// push transactions here

while (nextPhase  = 'N' !! nextPhase = 'n')
  for (i = 0; i < 1; i++){
    fprintf("");
   FILE *file;
      customername.append(".csv");       // add customer name
      file = fopen(customername);  
  }
  printf("Enter item code:");
  scanf("%d",itemCode);
  push (,itemCode);
  printf("Enter quantity of item:");
  scanf("%d",quantity);
  push (,quantity);

  printf("Item has been successfully added to your cart");
  while (nextPhase != 'Y' || nextPhase!= 'y' || nextPhase!= 'N' || nextPhase!= 'n')
    printf("Do you want to proceed to checkout? Y/N") ;
	  scanf("%c",nextPhase);
      if(nextPhase == 'Y' || nextPhase== 'y'){
        phase_4();
    }
   			
    	else if( nextPhase == 'N' || nextPhase == 'n'){
        system("cls");
        break;
  
     }  else{
        printf("Invalid entry, please enter Y or N");
        getch(nextPhase);     }
}