i = 1;
while(a.length > 1) // 0   //a=credit card number inputted
{ 
  //4417 1234 5678 9113
  //
  remainder = a%10 // first value; if divisible by 10 true, else false    
  a = 10(mod(a)) // update
  //4417 1234 5678 911
  //4417 1234 5678 91

  if(i = even)  //for every even digit
  num = remainder * 2
  b = num%10        //first digit of the product
  c =10(mod(num))   //second digit of the product
  sum = b + c + sum
  
  else          //for every odd digit
  sum = remainder + sum

i++
}
if(i = even)  //for every even digit
  num = remainder * 2
  b = num%10        //first digit of the product
  c =10(mod(num))   //second digit of the product
  sum = b + c + sum
  
  else          //for every odd digit
  sum = remainder + sum
  
if (divisible by 10 remainder of sum == 0)
  t
else
  invalid