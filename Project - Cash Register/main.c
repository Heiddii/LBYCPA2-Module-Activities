#include <stdio.h>
#include "module1.h"

int main(void){
  // login module y or n character to procceed for loop with 3 trials. after the three trial close program
  // for (i = 0; i < 3 ; i++)
  //   num = login() (this program brings back either 1 or 0. 0 = f, 1 = t)
//   if(num == 1) break



//if ( num == 1 ) execute the main code
// module 1 while loop. ask for the name
//    module 2 if the costumer pick the cash register this will execute
//      menu() prints the menu
//      stack or queue basically a data structure ( push or add value)
//      to move the next the program will ask if yes or no if yes procceed to module 3


//   module 3 ( receipt) print all that is in the array or the things inside the data structure
//   can comeback to module 2
//   has the pop coding or removing a value in the code it might use bubble sort or such if removing items
//   push all items to csv
//   move to module 4 


//   module 4(int totalcost, char name[]) input total cost and everything in this code will be push in the csv if aborted the code will start again this is also where the csv file printed or close



//   module 5 the datastructure will be emptied after that it will start the module 1  
}

